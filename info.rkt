#lang info

(define name "tui")
(define version "0.0.1")
(define description "Terminal UI library for Racket (Linux only)")
(define authors '("lu96-wow"))
(define license '(MIT))

(define collection "tui")
(define build-deps '("racket-lib" "scribble"))
(define compile-omit-paths '("demo"))